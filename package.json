{
  "name": "patent-forge",
  "version": "1.0.0",
  "description": "Patent Scout application with Express backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "cd frontend && npm install && npm run build",
    "install-deps": "npm install && cd frontend && npm install",
    "vercel-build": "npm run install-deps && npm run build"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
